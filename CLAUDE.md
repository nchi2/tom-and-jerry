# 톰과 제리 리메이크 — 코드 저장소

스타크래프트 유즈맵 "톰과 제리"(허허벌판/가스 버전) 리메이크의 **프로토타입 1**.
완성품이 아니라 **게임성 검증용 실험 장치**다. 제작: 남정현, 변준희 (팀 Sazir).

## 코어 규칙 (이 프로젝트의 전부)

> **적이 커서 좁은 틈을 못 지나간다.**
> 벽이 벽이라서 막는 게 아니라, 틈이 좁아서 막는다.

플레이어는 작은 햄스터다. 공격 수단이 없고, 벽만 세울 수 있다.
적은 몸집이 훨씬 크고 계속 쫓아온다. 닿으면 잡힌다.
이 한 문장이 손맛으로 느껴지는지 확인하는 것이 프로토타입 1의 목적이다.

## 저장소 두 곳 — 여기는 "코드" 쪽이다

기획·결정 문서는 이 저장소에 없다. **옵시디언 볼트에 있다.**

| | 경로 | 담는 것 |
|---|---|---|
| **코드 (지금 여기)** | `~/coding/private/tom-and-jerry/` | Three.js + Vite 프로토타입 |
| **기록** | `~/obsidian/Sazir-vault/` | 기획·결정·현황·백로그 |

볼트는 `.claude/settings.local.json`의 `additionalDirectories`로 등록돼 있어 **바로 읽을 수 있다.**

| 문서 | 내용 |
|---|---|
| `~/obsidian/Sazir-vault/CLAUDE.md` | 프로젝트 진입점 |
| `~/obsidian/Sazir-vault/tom-and-jerry/01-원작분석.md` | 원작 구조, 성립 조건, 결함 |
| `~/obsidian/Sazir-vault/tom-and-jerry/02-설계결정.md` | 결정과 근거. **변경 전 반드시 확인** |
| `~/obsidian/Sazir-vault/tom-and-jerry/03-현재상태.md` | 구현 현황 + 열린 검증 질문 |
| `~/obsidian/Sazir-vault/tom-and-jerry/04-백로그.md` | 보류 중인 기능과 그 이유 |

## 코드 구조

- `src/main.js` — 게임 전부 (약 7,100줄). 튜닝 파라미터 `P`가 파일 최상단
- `src/net.js` — 2P 접속만 (PeerJS, D92). **게임을 모른다** — 봉투와 바이트만 다룬다
- `index.html` — HUD/도움말/로비 DOM
- `tools/verify.js` — 회귀 하니스. 브라우저에서
  `fetch('/tools/verify.js').then(r=>r.text()).then(eval)` 후 `__verify()`.
  **A 150초 무입력 / B 명령·작업 경로 / D 2P 경제 / E 명령 계층 / C 전투.**
  변경할 때마다 돌리고 `errs: 0`을 확인할 것
- 개발 서버: `npm run dev` (기본 5173). `.claude/launch.json`은 `--port 5299 --strictPort`로 고정
- 디버그 훅: `window.__game` — `step(seconds)`로 헤드리스 진행 가능

코어 구현은 3겹이다. **직접 조정할 가능성이 높은 부분이므로 건드렸으면 상세히 보고할 것.**
1. 물리 — `collideWithObstacles` (원-AABB 밀어내기. 크기에서 통행권이 *나오게* 되어 있다)
2. 길찾기 — `computeClearance` + `planEnemyPath` (clearance ≥ 반지름인 칸만 밟음 = 시도조차 안 함)
3. 돌파 — `planEnemyPath` 후반 + 정체 감지 (우회로가 없을 때만 벽 공격)

## 2P가 만든 규칙 (D92) — 어기면 조용히 깨진다

- **`updateActor(p, dt)`는 `p`와 `p.in`만 읽는다.** `keys`·`mouseDown`·`activeCam()` 금지.
  카메라·키보드를 읽는 곳은 `sampleLocalInput` 하나뿐이다
- **시뮬을 바꾸는 유일한 입구는 `applyCommand(p, cmd)`다.** 여길 안 거치면
  그 동작이 참가자 화면에서 사라진다. 명령에는 격자 좌표와 엔티티 id만 담는다
- **스폰·재시작은 `netAuthoring()` 가드 안**(`makeEnemy`·`spawnPickup`·`advanceStage`·`setMap`).
  참가자가 id를 만들면 스냅샷이 엉뚱한 개체에 값을 써 넣고 **물리 버그처럼 보인다**
- **화면에 쓰는 값은 `player`가 아니라 `localPlayer()`** — 참가자 쪽에서는 그게 동료 슬롯이다
- 소유자가 있는 알림은 `flashMsg`가 아니라 **`flashFor(p, ...)`**

## 작업할 때 지킬 것

**결정에는 근거가 있다.**
`02-설계결정.md`의 항목(D1~D11)은 대부분 실제 플레이 후 도출된 것이다.
기존 결정과 어긋나는 방향이 더 나아 보이면, 바꾸기 전에 그 항목의 "왜"를 읽고
근거가 여전히 유효한지 판단한 뒤 진행하거나 물어봐 달라.

**지금 검증 중인 질문이 있다.**
`03-현재상태.md` 하단의 Q1~Q15가 열려 있는 것들이다. 지금 가장 위는 **Q15 — 둘이 하면
실제로 재미있는가**(D92). 새 기능이 이 질문에 답하는 데 도움이 되는지,
아니면 답을 흐리는지가 우선순위 기준이다.

**문서를 같이 갱신한다.**
구현을 바꿨으면 `03-현재상태.md`에, 새 결정을 내렸으면 `02-설계결정.md`에 같은 작업 안에서 반영한다.
코드만 바뀌고 문서가 그대로면 다음 세션이 틀린 전제로 판단하게 된다.

**튜닝 가능하게.**
플레이 감각에 영향을 주는 값은 인게임에서 조절 가능해야 한다.
새 파라미터는 `P`에 넣고 lil-gui 폴더에 슬라이더를 붙이고, `snapshotSettings`/`applySettings`에도 태운다.

**보고를 남겨 달라.**
변경 후 무엇을 어떻게 구현했는지 짧게. 코어 규칙의 구현 방식을 건드렸다면 특히 상세히.

> **2026-08-21 대조 완료.** 2P 협동(D92)이 붙었다 — `02-설계결정.md`에 D92 전문,
> `03-현재상태.md`에 조작·화면·알려진 이슈·Q15, D43·D47에는 "2P가 실제로 무엇을 실행시켰는지"
> 후기를 달았다. D7의 "잡히면 소멸"은 이제 **구현돼 있고 소유자별**이다.
> 다만 원칙은 그대로다 — 구현이 실제로 어떤지는 `src/main.js`가 진실이다.
